import 'package:flutter/material.dart';

class FreeAgentSettingScreen extends StatelessWidget {
  const FreeAgentSettingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Free Agent Setting Screen'),
    );
  }
}