import 'package:flutter/material.dart';

class FreeAgentGameScreen extends StatelessWidget {
  const FreeAgentGameScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Free Agent Game Screen'),
    );
  }
}