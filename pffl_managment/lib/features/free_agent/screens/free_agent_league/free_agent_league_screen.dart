import 'package:flutter/material.dart';

class FreeAgentLeagueScreen extends StatelessWidget {
  const FreeAgentLeagueScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Free Agent League Screen'),
    );
  }
}