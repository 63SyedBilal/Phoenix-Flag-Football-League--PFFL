import 'package:flutter/material.dart';

class RefereeSettingScreen extends StatelessWidget {
  const RefereeSettingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Referee Setting Screen'),
    );
  }
}