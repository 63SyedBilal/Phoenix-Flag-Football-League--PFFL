import 'package:flutter/material.dart';

class RefereeLeagueScreen extends StatelessWidget {
  const RefereeLeagueScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Referee League Screen'),
    );
  }
}