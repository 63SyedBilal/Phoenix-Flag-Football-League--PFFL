import 'package:flutter/material.dart';

class RefereeGameScreen extends StatelessWidget {
  const RefereeGameScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Referee Game Screen'),
    );
  }
}