import 'package:flutter/material.dart';

class StatSettingScreen extends StatelessWidget {
  const StatSettingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Stat Keeper Setting Screen'),
    );
  }
}