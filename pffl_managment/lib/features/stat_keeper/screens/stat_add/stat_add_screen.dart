import 'package:flutter/material.dart';

class StatAddScreen extends StatelessWidget {
  const StatAddScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Stat Keeper Add Screen'),
    );
  }
}