import 'package:flutter/material.dart';

class StatStatsScreen extends StatelessWidget {
  const StatStatsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Stat Keeper Stats Screen'),
    );
  }
}