import 'package:flutter/material.dart';

class StatGameScreen extends StatelessWidget {
  const StatGameScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Stat Keeper Game Screen'),
    );
  }
}