import 'package:flutter/material.dart';
import 'package:flutter_svg/flutter_svg.dart';

class SvgIcons {
  static Widget backArrow({double size = 20, Color? color}) {
    return SvgPicture.asset(
      'assets/icons/Back arrow.svg',
      height: size,
      width: size,
      colorFilter: color != null
          ? ColorFilter.mode(color, BlendMode.srcIn)
          : null,
    );
  }

  static Widget home({double size = 20, Color? color}) {
    return SvgPicture.asset(
      'assets/icons/home.svg',
      height: size,
      width: size,
      colorFilter: color != null
          ? ColorFilter.mode(color, BlendMode.srcIn)
          : null,
    );
  }

  static Widget homeunselected({double size = 20, Color? color}) {
    return SvgPicture.asset(
      'assets/icons/homeUnselected.svg',
      height: size,
      width: size,
      colorFilter: color != null
          ? ColorFilter.mode(color, BlendMode.srcIn)
          : null,
    );
  }

  static Widget homeFilled({double size = 20, Color? color}) {
    return SvgPicture.asset(
      'assets/icons/fillhome.svg',
      height: size,
      width: size,
      colorFilter: color != null
          ? ColorFilter.mode(color, BlendMode.srcIn)
          : null,
    );
  }

  static Widget leagues({double size = 20, Color? color}) {
    return SvgPicture.asset(
      'assets/icons/league.svg',
      height: size,
      width: size,
      colorFilter: color != null
          ? ColorFilter.mode(color, BlendMode.srcIn)
          : null,
    );
  }

  static Widget leaguesFilled({double size = 20, Color? color}) {
    return SvgPicture.asset(
      'assets/icons/fillleague.svg',
      height: size,
      width: size,
      colorFilter: color != null
          ? ColorFilter.mode(color, BlendMode.srcIn)
          : null,
    );
  }

  static Widget games({double size = 20, Color? color}) {
    return SvgPicture.asset(
      'assets/icons/games.svg',
      height: size,
      width: size,
      colorFilter: color != null
          ? ColorFilter.mode(color, BlendMode.srcIn)
          : null,
    );
  }

  static Widget gamesFilled({double size = 20, Color? color}) {
    return SvgPicture.asset(
      'assets/icons/fillgames.svg',
      height: size,
      width: size,
      colorFilter: color != null
          ? ColorFilter.mode(color, BlendMode.srcIn)
          : null,
    );
  }

  static Widget users({double size = 20, Color? color}) {
    return SvgPicture.asset(
      'assets/icons/users.svg',
      height: size,
      width: size,
      colorFilter: color != null
          ? ColorFilter.mode(color, BlendMode.srcIn)
          : null,
    );
  }

  static Widget usersFilled({double size = 20, Color? color}) {
    return SvgPicture.asset(
      'assets/icons/fillusers.svg',
      height: size,
      width: size,
      colorFilter: color != null
          ? ColorFilter.mode(color, BlendMode.srcIn)
          : null,
    );
  }

  static Widget setting({double size = 20, Color? color}) {
    return SvgPicture.asset(
      'assets/icons/tdesign_setting.svg',
      height: size,
      width: size,
      colorFilter: color != null
          ? ColorFilter.mode(color, BlendMode.srcIn)
          : null,
    );
  }

  static Widget settingFilled({double size = 20, Color? color}) {
    return SvgPicture.asset(
      'assets/icons/fillsettings.svg',
      height: size,
      width: size,
      colorFilter: color != null
          ? ColorFilter.mode(color, BlendMode.srcIn)
          : null,
    );
  }

  static Widget add({double size = 20, Color? color}) {
    return SvgPicture.asset(
      'assets/icons/add.svg',
      height: size,
      width: size,
      colorFilter: color != null
          ? ColorFilter.mode(color, BlendMode.srcIn)
          : null,
    );
  }

  static Widget addFilled({double size = 20, Color? color}) {
    return SvgPicture.asset(
      'assets/icons/add.svg',
      height: size,
      width: size,
      colorFilter: color != null
          ? ColorFilter.mode(color, BlendMode.srcIn)
          : null,
    );
  }

  static Widget frame1({double size = 20, Color? color}) {
    return SvgPicture.asset(
      'assets/icons/Frame-1.svg',
      height: size,
      width: size,
      colorFilter: color != null
          ? ColorFilter.mode(color, BlendMode.srcIn)
          : null,
    );
  }

  static Widget frame2({double size = 20, Color? color}) {
    return SvgPicture.asset(
      'assets/icons/Frame (2).svg',
      height: size,
      width: size,
      colorFilter: color != null
          ? ColorFilter.mode(color, BlendMode.srcIn)
          : null,
    );
  }

  static Widget frame3({double size = 20, Color? color}) {
    return SvgPicture.asset(
      'assets/icons/Frame-3.svg',
      height: size,
      width: size,
      colorFilter: color != null
          ? ColorFilter.mode(color, BlendMode.srcIn)
          : null,
    );
  }

  static Widget frame4({double size = 20, Color? color}) {
    return SvgPicture.asset(
      'assets/icons/Frame-4.svg',
      height: size,
      width: size,
      colorFilter: color != null
          ? ColorFilter.mode(color, BlendMode.srcIn)
          : null,
    );
  }

  static Widget frame5({double size = 20, Color? color}) {
    return SvgPicture.asset(
      'assets/icons/Frame-5.svg',
      height: size,
      width: size,
      colorFilter: color != null
          ? ColorFilter.mode(color, BlendMode.srcIn)
          : null,
    );
  }

  static Widget frame6({double size = 20, Color? color}) {
    return SvgPicture.asset(
      'assets/icons/Frame-6.svg',
      height: size,
      width: size,
      colorFilter: color != null
          ? ColorFilter.mode(color, BlendMode.srcIn)
          : null,
    );
  }

  static Widget frame7({double size = 20, Color? color}) {
    return SvgPicture.asset(
      'assets/icons/Frame-7.svg',
      height: size,
      width: size,
      colorFilter: color != null
          ? ColorFilter.mode(color, BlendMode.srcIn)
          : null,
    );
  }

  static Widget frame8({double size = 20, Color? color}) {
    return SvgPicture.asset(
      'assets/icons/Frame-8.svg',
      height: size,
      width: size,
      colorFilter: color != null
          ? ColorFilter.mode(color, BlendMode.srcIn)
          : null,
    );
  }

  static Widget frame9({double size = 20, Color? color}) {
    return SvgPicture.asset(
      'assets/icons/Frame-9.svg',
      height: size,
      width: size,
      colorFilter: color != null
          ? ColorFilter.mode(color, BlendMode.srcIn)
          : null,
    );
  }

  static Widget frame10({double size = 20, Color? color}) {
    return SvgPicture.asset(
      'assets/icons/Frame-10.svg',
      height: size,
      width: size,
      colorFilter: color != null
          ? ColorFilter.mode(color, BlendMode.srcIn)
          : null,
    );
  }

  static Widget vector({double size = 20, Color? color}) {
    return SvgPicture.asset(
      'assets/icons/Vector.svg',
      height: size,
      width: size,
      colorFilter: color != null
          ? ColorFilter.mode(color, BlendMode.srcIn)
          : null,
    );
  }

  static Widget fluentTrophy({double size = 20, Color? color}) {
    return SvgPicture.asset(
      'assets/icons/fluent_trophy-32-regular.svg',
      height: size,
      width: size,
      colorFilter: color != null
          ? ColorFilter.mode(color, BlendMode.srcIn)
          : null,
    );
  }

  static Widget infoFill({double size = 20, Color? color}) {
    return SvgPicture.asset(
      'assets/icons/info-fill 1.svg',
      height: size,
      width: size,
      colorFilter: color != null
          ? ColorFilter.mode(color, BlendMode.srcIn)
          : null,
    );
  }

  static Widget icon1({double size = 20, Color? color}) {
    return SvgPicture.asset(
      'assets/icons/Media/Icon-1.svg',
      height: size,
      width: size,
      colorFilter: color != null
          ? ColorFilter.mode(color, BlendMode.srcIn)
          : null,
    );
  }

  static Widget emailAfterInvitation({double size = 22, Color? color}) {
    return SvgPicture.asset(
      'assets/icons/emailiconafterinvitationsand.svg',
      height: size,
      width: size,
      colorFilter: color != null
          ? ColorFilter.mode(color, BlendMode.srcIn)
          : null,
    );
  }
}
