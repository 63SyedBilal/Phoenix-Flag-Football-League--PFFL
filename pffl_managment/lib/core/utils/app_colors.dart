import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF0F173E);
  static const Color primaryLight = Color(
    0xFF10B981,
  ); // Green color used in success messages

  // Text colors
  static const Color textPrimary = Color(0xFF111827);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textDisabled = Color(0xFF9CA3AF);
  static const Color textBlack = Color(0xFF000000);
  static const Color textGrey = Colors.grey;
  static const Color red = Colors.red;

  // Background colors
  static const Color backgroundWhite = Colors.white;

  // Border colors
  static const Color borderDefault = Color(0xFFD1D5DB);
  static const Color borderLight = Color(0xFFE5E7EB);

  // Shadow colors
  static const Color shadowDefault = Color(0xFF101828);

  // Icon colors
  static const Color iconEmailActive = Color(
    0xFF4285F4,
  ); // Blue color for active email icons
  static const Color iconEmailInactive = Color(
    0xFF000000,
  ); // Black color for inactive email icons

  // Button colors
  static const Color buttonBackground = Color(0xFF0F173E);
  static const Color buttonText = Colors.white;

  // Chip colors
  static const Color chipBackground = Color(0xFFE0E0E0);

  // Position button colors
  static const Color positionButtonBackground = Color(0xFFADD8E6); // Light blue

  // Success colors
  static const Color success = Color(0xFF10B981);

  // Step indicator colors
  static const Color stepActive = Color(0xFF0F173E);
  static const Color stepInactive = Color(0xFFE5E7EB);
}
